# =============================================================================
# SVUP Notifications App - Environment Variables Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Astro DB (Turso) Configuration
# -----------------------------------------------------------------------------
# Remote database connection for libSQL/Turso
# Get these values from: https://turso.tech/app
# Required for: Database operations (FCM tokens, notifications storage)
ASTRO_DB_REMOTE_URL=libsql://your-database.turso.io
ASTRO_DB_APP_TOKEN=your-turso-token

# -----------------------------------------------------------------------------
# External Backend API
# -----------------------------------------------------------------------------
# SVUP Backend API endpoint for authentication
# Used for: Login/logout operations, TOTP validation
# Default: https://firebackend-6c859a037589.herokuapp.com/api
BACKEND_API_URL=https://firebackend-6c859a037589.herokuapp.com/api

# -----------------------------------------------------------------------------
# Firebase Client Configuration (PUBLIC - Exposed to Browser)
# -----------------------------------------------------------------------------
# These variables are prefixed with PUBLIC_ and will be available in client-side code
# Get these values from: Firebase Console > Project Settings > General
# Required for: Client-side FCM initialization, push notifications

# Firebase API Key (safe to expose publicly)
# Format: AIzaSy... (starts with AIza)
# NOT the same as VAPID key!
PUBLIC_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Firebase Auth Domain (format: your-project.firebaseapp.com)
PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com

# Firebase Project ID
PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# Firebase Storage Bucket (format: your-project.appspot.com)
PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# Firebase Messaging Sender ID (numeric)
PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id

# Firebase App ID (format: 1:xxx:web:xxx)
PUBLIC_FIREBASE_APP_ID=your-app-id

# Firebase VAPID Key for Web Push (Base64 string)
# Get from: Firebase Console > Project Settings > Cloud Messaging > Web Push certificates
# Format: Base64 string (NOT the same as API Key!)
PUBLIC_FIREBASE_VAPID_KEY=BGU6HXc8S6g-XXXXXXXXXXXXXXXXXXXXXXXXXXX

# -----------------------------------------------------------------------------
# Firebase Admin SDK Configuration (SERVER-SIDE ONLY - Keep Secret!)
# -----------------------------------------------------------------------------
# These variables are used server-side only and should NEVER be exposed to clients
# Get these values from: Firebase Console > Project Settings > Service Accounts
# Required for: Sending push notifications via webhook endpoint

# Firebase Project ID (same as PUBLIC_FIREBASE_PROJECT_ID)
FIREBASE_PROJECT_ID=your-project-id

# Service Account Email (format: firebase-adminsdk-xxx@your-project.iam.gserviceaccount.com)
FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Service Account Private Key (include quotes and newlines as shown)
# Important: Keep the \n characters for line breaks in the key
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----\n"

# =============================================================================
# Deployment Instructions
# =============================================================================
# 
# For Netlify Deployment:
# 1. Go to: Site Settings > Environment Variables
# 2. Add all variables listed above with your actual values
# 3. Ensure FIREBASE_PRIVATE_KEY includes the quotes and \n characters
# 4. Deploy or trigger a rebuild after adding variables
#
# Security Notes:
# - Never commit .env file to git (already in .gitignore)
# - Rotate FIREBASE_PRIVATE_KEY if accidentally exposed
# - Keep ASTRO_DB_APP_TOKEN secure (has read/write access to database)
# - PUBLIC_* variables are safe to expose (designed for client-side use)
# =============================================================================
